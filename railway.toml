# Railway reads this file for ALL services in the project.
# Keep this file minimal — only settings safe for every service.
# Service-specific Dockerfile paths are set in the Railway dashboard.

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
